# 유튜브 영상 목록 캐시 — 할당량 아끼려고 로컬에만 둡니다
tools/.yt-cache.json
__pycache__/
*.pyc

# 관리자 계정 (해시). 서버에만 두고 저장소에는 올리지 않습니다
admin/config.php
# FTP 접속 정보와 배포 기록 — 서버에만 두고 저장소에는 올리지 않습니다
data/deploy.json
data/.deploy-state.json
# 유튜브 API 키 — 서버/PC 에만 둡니다
data/youtube.json

# 슬랙 웹훅 주소와 알림 상태 — 서버/PC 에만 둡니다
data/slack.json
data/.notify-state.json

# 채팅 로그·당첨자 명단 — 시청자 닉네임이 있어 이 PC 에만 둡니다
data/chat/
data/prizes/
balloon-catch.log
