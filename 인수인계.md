# 인수인계 — 스타크래프트 기록실

새로 시작하는 Claude Code 가 이 문서 하나만 읽고 이어받을 수 있게 정리했습니다.
날짜 기준: 2026-08-19.

---

## 0. 제일 먼저 알아야 할 것

**실제 사이트는 카페24의 pubgin.com 입니다. GitHub 은 백업일 뿐입니다.**
GitHub 에 올린다고 사이트가 바뀌지 않습니다. 사이트를 바꾸려면 FTP 로
올려야 하고, 그건 `tools/deploy.py` 가 합니다.

**클라우드(웹) 작업방에서는 pubgin.com 에 닿지 못합니다.**
환경의 네트워크 정책이 막고 있습니다 — 프록시가 CONNECT 에 403 을 돌려주고,
FTP(21)·SSH(22)·FTPS(990)도 전부 막혀 있습니다. 회사 PC 에서는 제한이
없으므로, 업로드가 필요한 일은 PC 에서 도는 작업방이어야 합니다.

**GitHub 푸시도 웹 작업방에서는 403 입니다.** 세션 시작 때 정해지는 권한이라
도중에 바뀌지 않습니다. PC 에서 `git push` 하시면 됩니다.

---

## 1. 무엇을 만들었나

정적 사이트 세 개가 한 폴더에 있습니다.

| | 주소 | 규모 |
| --- | --- | --- |
| 끝장전 기록실 | `pubgin.com/endgame/` | 296매치 · 2,656세트 · 선수 35명 |
| ASL 기록실 | `pubgin.com/endgame/asl/` | 23대회 · 1,299매치 · 2,177세트 · 선수 95명 |
| CG 제작 툴 | `pubgin.com/endgame/admin/` | 대진표 이미지 (관리자 로그인) |

### 끝장전 기록실
연도 탭, 선수 랭킹, 맵 통계, 경기 기록, 기록실(각종 최고기록).
선수마다 개별 페이지 `p/<슬러그>.html` 35개.
CSV 13개·XLSX 2개 내려받기와 구글시트 연동 안내(`sheets.html`).

### ASL 기록실
대회, 선수 랭킹, 선수 명단, 맵 통계, 경기 기록, **상대 전적**, 기록실.
선수 페이지 95개(`asl/p/`), 대회 페이지 23개(`asl/t/`).

**상대 전적 탭**이 이번에 새로 들어간 것입니다. 선수 vs 선수, 종족 vs 종족,
맵·대회 필터를 자유롭게 섞습니다. 세트 전적과 승률, 맵별·대회별·종족대결별
표, 세트 하나하나의 목록이 나옵니다.

### CG 제작 툴
1920×1080 대진표 PNG 생성. PHP 세션 로그인 뒤에 있습니다.
**메뉴에 없습니다** — 페이지 맨 아래 톱니바퀴로만 들어갑니다. 일부러
눈에 안 띄게 둔 것이니 메뉴로 되돌리지 마세요.

---

## 2. 폴더 구조

```
data/          정본. 빌드가 절대 고치지 않습니다
  endgame.json     끝장전 (수동으로 넣은 것)
  asl.json         ASL (구글시트에서 받아옴)
  asl-source.json  그 구글시트 주소
  site.json        baseUrl
  videos.json      경기 → 유튜브 매핑 (지금 비어 있음)
  deploy.json      FTP 비밀번호 — 저장소에 없음
  youtube.json     유튜브 API 키 — 저장소에 없음

tools/         고칠 곳은 전부 여기
  build.py         전체 빌드 (오케스트레이터)
  render.py        HTML 만들기
  stats.py         끝장전 파생 통계
  asl_import.py    시트/엑셀 → asl.json
  slug.py          한글 → 로마자 슬러그
  audit.py         경로 이식성 검사
  deploy.py        FTP 업로드 (바뀐 파일만)
  update.py        시트 → 데이터 → 빌드 → 업로드
  setup.py         새 PC 첫 준비
  start.py         START.bat 이 부르는 것 (처음부터 끝까지)
  fetch_videos.py  유튜브 다시보기 매칭
  admin_php.py     관리자 로그인 PHP
  site.css / app.js / asl_app.js / cg_app.js

img/players/   선수 사진을 넣는 곳 (지금은 비어 있음)

빌드 결과물 — 직접 고치지 마세요
  index.html  p/  asl/  admin/  csv/  xlsx/  sheets.html
```

---

## 3. 명령

```bash
python3 tools/start.py     # 처음부터 끝까지 (START.bat 이 부름)
python3 tools/setup.py     # 준비만 (FTP 정보·접속 확인)

python3 tools/update.py              # 시트 → 데이터 → 사이트 → 업로드
python3 tools/update.py --dry-run    # 바꾸지 않고 차이만
python3 tools/build.py               # 사이트만 다시 만들기
python3 tools/deploy.py              # 만든 것 올리기 (바뀐 파일만)
```

슬래시 명령도 있습니다 — `/갱신` `/확인` `/배포` `/상태`
(`.claude/commands/` 에 있습니다)

---

## 4. 새 PC 에서 처음 시작할 때

1. 이 폴더를 통째로 놓습니다 (`.git` 포함)
2. `START.bat` 더블클릭 (윈도우) 또는 `python3 tools/start.py`
3. FTP 정보 네 가지만 넣으면 나머지는 알아서 합니다

`START.bat` 이 하는 일 — 예전 폴더에서 유튜브 키 찾기 → 파이썬·openpyxl
확인 → FTP 정보 저장 → FTP·시트 확인 → 영상 채우기 → 빌드·업로드 →
GitHub 백업. 중간에 안 되는 게 있어도 멈추지 않고 마지막에 남은 것을
알려 줍니다. **두 번째 실행부터는 아무것도 묻지 않습니다.**

### 새 PC 에서 다시 만들어야 하는 것

저장소에 없는 것들입니다.

| 파일 | 내용 | 다시 만드는 법 |
| --- | --- | --- |
| `data/deploy.json` | 카페24 FTP 정보 | `setup.py` 가 물어봅니다 |
| `data/youtube.json` | 유튜브 API 키 | `start.py` 가 예전 폴더에서 찾아옵니다 |
| `admin/config.php` | 관리자 계정 | 서버에만 있음. `/admin/` 첫 접속 때 정합니다 |

---

## 5. 아직 안 된 것

- **끝장전 296경기 전부 다시보기 영상이 비어 있습니다.**
  `data/videos.json` 이 비어 있습니다. 유튜브 API 키를 넣고
  `python3 tools/fetch_videos.py --write` 를 돌리면 채워집니다.
  키가 `data/youtube.json` 에 있거나 `YOUTUBE_API_KEY` 환경변수면 됩니다.
- **끝장전은 자동 갱신 경로가 없습니다.** ASL 은 구글시트에서 받아오는데,
  끝장전 `data/endgame.json` 은 수동으로 넣은 것입니다. 시트 주소를
  받으면 `asl_import.py` 와 같은 방식으로 붙일 수 있습니다.
- **선수 사진이 한 장도 없습니다.** 넣는 법은 아래 7번.
- **eloboard.com 을 참고하지 못했습니다.** 사장님이 참고하라고 하셨는데
  이 환경에서 막혀 있어 못 봤습니다. 화면을 받으면 반영할 수 있습니다.
- **GitHub 에 이번 작업이 안 올라가 있습니다.** 브랜치
  `claude/starcraft-endgame-site-049jip` 에 커밋 19개가 로컬에만 있습니다.
  PC 에서 `git push -u origin claude/starcraft-endgame-site-049jip` 하면 됩니다.

---

## 6. 반드시 지킬 것

- **`index.html`, `p/`, `asl/`, `admin/`, `csv/`, `xlsx/`, `sheets.html` 은
  빌드 결과물입니다.** 직접 고치지 마세요. 고칠 곳은 `tools/` 와 `data/`.
- **`tools/build.py` 의 `if __name__ == '__main__'` 블록은 항상 파일 맨 끝**에
  있어야 합니다. 그 뒤에 함수를 덧붙이면 NameError 가 납니다. (두 번 겪었습니다)
- **`data/deploy.json`, `data/youtube.json`, `admin/config.php` 는 저장소에도
  웹서버에도 올리지 않습니다.** `deploy.py` 의 `NEVER_UPLOAD` 에 들어 있습니다.
  `sheets.html` 이 원본 JSON 을 공개하느라 `data/` 가 통째로 업로드되기
  때문에, `data/` 안에 비밀이 생기면 반드시 거기 추가해야 합니다.
- **`asl.json` 의 `players`·`maps`·`tournaments` 배열 순서를 바꾸지 마세요.**
  상대 전적이 쓰는 `setList` 가 그 자리 번호로 세트를 가리킵니다.
- **`update.py` 는 기록이 줄면 멈춥니다.** 시트에서 줄이 지워지는 사고를
  막기 위해서입니다. 사용자가 의도한 삭제라고 확인해 주기 전에는 `--force` 를
  붙이지 마세요.
- **작업이 끝나면 따로 시키지 않아도 올립니다.** 화면·기능을 고쳤으면
  `build.py && deploy.py`, 기록이 바뀌었으면 `update.py`. 사장님이 그렇게
  해 달라고 하셨습니다. 다만 서버에 못 닿으면 우회하지 말고 그대로 알리세요.

---

## 7. 선수 사진 넣는 법

`img/players/<슬러그>.jpg` 로 넣고 빌드하면 선수 페이지와 CG 툴에 자동으로
붙습니다. 확장자는 jpg·png·webp 다 됩니다. 정사각형 400×400 이상을 권합니다.

**어떤 이름으로 넣어야 하는지는 빌드가 `img/players/_사진목록.txt` 에
95명분을 적어 둡니다.** 예: 김택용 → `kim-taekyong.jpg`

끝장전 35명 슬러그가 ASL 95명과 전부 같아서, 사진 한 장이 양쪽에 쓰입니다.

CG 툴에서 선수 이름을 고르면 등록된 사진이 저절로 올라옵니다. 직접 올린
사진이 있으면 그쪽이 우선이고, 사진 없는 선수로 바꾸면 앞 선수 얼굴은
지워집니다.

---

## 8. 알아 둘 사실 (데이터)

- 끝장전은 사실상 9세트 고정입니다 (296경기 중 294경기).
- ASL 은 경기 날짜가 없어 대회·라운드 순서로만 정리합니다.
- ASL 동족전 549세트는 종족 상성 집계에서 빠집니다 (이기고 지는 종족이
  같아서 의미가 없습니다).
- **ASL 시트에서 이영호가 9줄만 P·Z 로 적혀 있습니다.** 빌드 때 T 로
  맞추고, 원본 시트는 건드리지 않으며, 실행할 때마다 알려 줍니다.
- ASL 매치(시리즈)는 같은 라운드에서 같은 두 선수가 연달아 치른 세트를
  하나로 묶은 것입니다.

---

## 9. 이번에 잡은 버그 (다시 만들지 마세요)

| 버그 | 증상 | 원인 |
| --- | --- | --- |
| 슬러그 종성표 27자 | 박상현 → `park-sathyeon` | JONG 표는 **정확히 28개**여야 합니다 |
| `.spoiler{display:inline-block}` | "결과 보기" 가 안 숨겨짐 | `[hidden]{display:none !important}` 로 해결 |
| FTPS 폴백이 죽음 | `TypeError` | `ftplib.all_errors` 는 이미 튜플. 다시 감싸면 안 됩니다 |
| 배포에 `.claude/`·`.md` 포함 | 내부 문서가 웹에 공개 | 점폴더·`.md`·`.bat` 제외 |
| `data/youtube.json` 업로드 | **API 키가 웹에 공개될 뻔** | `NEVER_UPLOAD` 에 추가 |
| CG 툴 사진 잔상 | 앞 선수 얼굴이 남음 | 등록사진→없는 선수로 바꿀 때 지우도록 |
| `START.bat` 이 LF | cmd 가 줄을 잘못 읽어 통째로 실패 | 배치 파일은 **CRLF + ASCII** |
| ASL 라운드 'WILD CARD' | 대회명이 쪼개짐 | `TOURNEY_RE` + `ROUND_ALIAS` |

---

## 10. 새 작업방에서 첫 마디로 하면 좋은 것

```
CLAUDE.md 와 인수인계.md 를 읽고, python3 tools/build.py 를 한 번 돌려서
지금 상태가 정상인지 확인해 줘.
```

빌드가 "경로 검사 통과" 까지 나오면 정상입니다.
