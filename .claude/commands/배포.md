---
description: 사이트를 다시 만들어 FTP 로 올립니다 (데이터는 건드리지 않음)
---

데이터는 그대로 두고 화면만 다시 만들어 올립니다.

```bash
python3 tools/build.py
python3 tools/deploy.py
```

`build.py` 끝에 나오는 **경로 검사** 결과를 꼭 확인하고, 통과하지 못했으면
올리지 말고 무엇이 걸렸는지 알려 주세요.

올린 뒤에는 파일 몇 개가 올라갔는지 한 줄로 전해 주세요.
