# 회사 PC 연동 — 순서대로

한 번만 하면 됩니다. 그 뒤로는 휴대폰에서 `/갱신` 만 치면 시트부터
pubgin.com 까지 알아서 돌아갑니다.

전체 30분쯤 걸립니다. 1~4번은 PC 앞에서, 5번부터는 휴대폰으로도 됩니다.

---

## 1. 폴더 놓기

받은 `sc-endgame-전체(도구포함).zip` 을 풀어서 PC 에 둡니다.
위치는 아무 데나 괜찮지만, 나중에 찾기 쉬운 곳이 좋습니다.

```
C:\work\sc-endgame        (윈도우)
~/work/sc-endgame         (맥)
```

**이미 PUBG IN 폴더를 두신 곳 옆에 나란히 두시는 걸 권합니다.**

---

## 2. 파이썬 확인

명령 프롬프트(윈도우) 나 터미널(맥) 을 열고:

```bash
python3 --version
```

`3.8` 이상이 나오면 됩니다.
`python3 을 찾을 수 없습니다` 가 나오면 python.org 에서 받아 설치하시고,
설치할 때 **Add Python to PATH** 를 꼭 체크하세요.

---

## 3. 준비 스크립트 돌리기 ← 여기가 핵심

폴더로 들어가서 한 줄만 치면 됩니다.

```bash
cd C:\work\sc-endgame
python3 tools/setup.py
```

물어보는 것은 네 가지입니다. FileZilla 에 넣으신 그 정보 그대로입니다.

| 물어보는 것 | 넣을 것 |
| --- | --- |
| 호스트 | `pubgin.com` |
| 아이디 | 카페24 FTP 아이디 |
| 비밀번호 | 카페24 FTP 비밀번호 (화면에 안 보입니다 — 정상입니다) |
| 올릴 폴더 | `/www/endgame` |

그러면 스크립트가 알아서 여섯 가지를 확인합니다.

```
1. 파이썬            버전 확인
2. 엑셀 라이브러리    없으면 설치
3. FTP 접속 정보      방금 넣은 것을 data/deploy.json 에 저장 (600 권한)
4. FTP 접속 시험      진짜로 붙어 보고 /www/endgame 을 열어 봅니다
5. 구글시트 시험      ASL 시트를 진짜로 받아 봅니다
6. 빌드 시험          사이트를 한 번 만들어 봅니다
```

전부 ✅ 면 끝입니다. ❌ 가 있으면 **무엇을 고쳐야 하는지 화면에 나옵니다.**

> **4번에서 막히면** 거의 IP 문제입니다.
> 카페24 관리자 → 보안관리 → **FTP/Shell 접속설정** 에서
> 회사 IP 를 허용 목록에 넣어 주세요.
> (FileZilla 가 되는 PC 라면 보통 그냥 됩니다.)

이 스크립트는 몇 번을 돌려도 괜찮습니다. 이미 된 것은 건너뜁니다.

---

## 4. 한 번 올려 보기

```bash
python3 tools/update.py --dry-run
```

무엇이 달라지는지만 보여 주고 **아무것도 바꾸지 않습니다.** 확인이 되면:

```bash
python3 tools/update.py
```

구글시트 → 데이터 → 사이트 → FTP 업로드까지 한 번에 갑니다.
바뀐 파일만 올라가서 보통 몇 초면 끝납니다.

끝나면 https://pubgin.com/endgame/ 을 새로고침해서 확인하세요.

---

## 5. 휴대폰에서 쓰게 만들기

여기까지 하면 PC 는 다 됐습니다. 이제 휴대폰과 이어 붙입니다.

### 5-1. Claude Code 설치와 로그인

PC 에 Claude Code 가 없다면 설치하고, 폴더 안에서 로그인합니다.

```bash
cd C:\work\sc-endgame
claude
```

처음 켜면 이 폴더를 믿어도 되는지 묻습니다 — **예** 를 고르세요.
로그인이 안 돼 있으면 `/login` 을 치고 claude.ai 계정으로 들어갑니다.

### 5-2. 원격 조종 켜기

Claude Code 안에서:

```
/remote-control
```

QR 코드와 주소가 뜹니다. **휴대폰 카메라로 QR 을 찍으면** 바로 연결됩니다.

터미널에서 바로 켜고 싶으면 이렇게 해도 같습니다.

```bash
claude --remote-control "스타 기록실"
```

이름을 붙여 두면 휴대폰 목록에서 찾기 쉽습니다.

### 5-3. 휴대폰에서 열기

Claude 앱 → 아래 **Code** → 목록에서 `스타 기록실` 을 누릅니다.
컴퓨터 아이콘에 초록 점이 켜져 있으면 연결된 것입니다.

### 5-4. 켤 때마다 자동으로

매번 `/remote-control` 을 치기 귀찮으면 Claude Code 안에서 `/config` 를 열고
**Enable Remote Control for all sessions** 를 켜 두세요.
그다음부터는 그냥 `claude` 만 켜 두면 휴대폰에서 바로 잡힙니다.

> PC 가 켜져 있고 Claude Code 가 떠 있어야 휴대폰에서 잡힙니다.
> 절전 모드로 들어가면 끊깁니다 — 오래 돌릴 일이 있으면 절전을 꺼 두세요.

---

## 6. 이제 휴대폰에서 할 수 있는 것

| 치면 | 하는 일 |
| --- | --- |
| `/갱신` | 구글시트 → 사이트 → 업로드까지 한 번에 |
| `/확인` | 바꾸지 않고 무엇이 달라졌는지만 |
| `/배포` | 만들어 둔 것을 올리기만 |
| `/상태` | 지금 데이터가 어떤 상태인지 |

말로 시켜도 됩니다 — "시트 새로 받아서 올려 줘" 같은 식으로요.

---

## 7. GitHub 백업 올리기 (급하지 않음)

사이트와는 상관없습니다. **GitHub 에 안 올려도 pubgin.com 은 잘 돌아갑니다.**
소스 백업일 뿐이라 시간 나실 때 하시면 됩니다.

지금 커밋 11개가 제 쪽에만 쌓여 있고 올라가지 못했습니다. 제가 쓰는 접근
권한이 **읽기 전용**이라 밀어 넣을 수가 없습니다. 두 가지 중 편한 쪽으로요.

**(가) 회사 PC 에서 직접** — 폴더에서 한 줄이면 됩니다.

```bash
cd C:\work\sc-endgame
git push -u origin claude/starcraft-endgame-site-049jip
```

**(나) 저에게 쓰기 권한 주기** — https://claude.ai/settings 의 GitHub 연결에서
`hspark-art/sc-endgame` 에 쓰기 권한을 주시면 다음부터는 제가 올립니다.

---

## 알아 두실 것

- **시트에서 기록이 줄면 `update.py` 가 멈춥니다.** 실수로 줄이 지워진 것을
  사이트에 그대로 반영하지 않기 위해서입니다. 정말로 지우신 것이면
  `--force` 를 붙여 다시 돌리시면 됩니다.
- **`data/deploy.json` 과 `admin/config.php` 는 저장소에 올라가지 않습니다.**
  FTP 비밀번호와 관리자 계정이 들어 있어서입니다. PC 를 새로 쓰시면
  `tools/setup.py` 를 다시 돌리시면 됩니다.
- **관리자 로그인은 처음 한 번 직접 만듭니다.** https://pubgin.com/endgame/admin/
  에 들어가면 아이디와 비밀번호를 정하라고 나옵니다. 정하고 나면 그 뒤로는
  그 계정으로만 들어갑니다.
- 설명 문서(.md)는 웹서버에 올라가지 않습니다.
