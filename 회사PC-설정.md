# 회사 PC — 세 단계면 끝납니다

## 1. 압축 풀기

받은 `sc-endgame-전체(도구포함).zip` 을 풀어 주세요.

**바탕화면 말고** 이런 곳에 풀어 주세요.

```
C:\Users\ETALENT-NEW\scetalent-new
```

> 압축을 풀 때 **숨김 파일도 같이** 풀려야 합니다.
> 알집이면 "숨김 파일 표시" 를 켜 주세요. 윈도우 기본 압축은 그냥 됩니다.

기존 `scetalent` 폴더는 **그대로 두세요.** 거기 있는 유튜브 키를
새 폴더가 알아서 찾아옵니다.

---

## 2. `START.bat` 더블클릭

풀린 폴더 안에 `START.bat` 이 있습니다. 두 번 누르면 검은 창이 열리고
알아서 진행됩니다.

중간에 **딱 네 가지만 물어봅니다.** FileZilla 에 넣으셨던 그 정보입니다.

| 물어보는 것 | 넣을 것 |
| --- | --- |
| 호스트 | `pubgin.com` (그냥 엔터 치면 이게 들어갑니다) |
| 아이디 | 카페24 FTP 아이디 |
| 비밀번호 | 카페24 FTP 비밀번호 |
| 올릴 폴더 | `/www/endgame` (그냥 엔터) |

**비밀번호는 쳐도 화면에 안 보입니다. 고장 난 게 아니니 그냥 치고 엔터하세요.**

그다음은 다 알아서 합니다.

```
0. 예전 폴더에서 유튜브 키 찾아오기
1. 파이썬 확인
2. 엑셀 라이브러리 설치
3. FTP 정보 저장
4. FTP 접속 확인
5. 구글시트 확인
6. 다시보기 영상 채우기
7. 사이트 만들어 pubgin.com 에 올리기
8. GitHub 백업
```

끝나면 **무엇이 됐고 무엇이 안 됐는지** 화면에 나옵니다.
안 된 게 있으면 그 줄을 그대로 저에게 보여 주시면 됩니다.

> 파이썬이 없다고 나오면 https://www.python.org/downloads/ 에서 받아
> 설치해 주세요. 설치 화면 맨 아래 **"Add Python to PATH"** 를 꼭 체크하시고,
> 끝나면 `START.bat` 을 다시 더블클릭하시면 됩니다.

---

## 3. 휴대폰 연결

같은 검은 창에서 이렇게 치세요.

```
claude
```

들어가면 `/remote-control` 이라고 치세요. **QR 코드가 나옵니다.**
휴대폰 카메라로 찍으면 연결됩니다.

Claude 앱 → 아래 **Code** 탭에 들어가면 목록에 보입니다.

---

# 다 됐습니다

앞으로 ASL 기록이 시트에 추가되면, 휴대폰에서 이렇게만 치시면 됩니다.

| 치면 | 하는 일 |
| --- | --- |
| `/갱신` | 시트 → 사이트 → 업로드까지 한 번에 |
| `/확인` | 바꾸지 않고 무엇이 달라졌는지만 |
| `/배포` | 만들어 둔 것 올리기만 |
| `/상태` | 지금 데이터 상태 |

말로 시키셔도 됩니다 — "시트 새로 받아서 올려 줘" 처럼요.

PC 에서 하실 때는 `START.bat` 을 다시 더블클릭하시면 됩니다.
두 번째부터는 아무것도 안 물어보고 바로 갱신합니다.

---

## 혹시 막히면

**FTP 접속이 안 될 때** — 거의 IP 문제입니다.
카페24 관리자 → 보안관리 → **FTP/Shell 접속설정** 에서 회사 IP 를
허용 목록에 넣어 주세요.

**GitHub 백업이 안 될 때** — 사이트와 상관없으니 그냥 두셔도 됩니다.
아이디와 토큰을 물어보면 넣어 주시면 되고, 토큰은 github.com →
Settings → Developer settings → Personal access tokens 에서
`repo` 권한으로 만드시면 됩니다.

**관리자(CG 제작) 로그인** — https://pubgin.com/endgame/admin/ 에 처음
들어가시면 아이디와 비밀번호를 정하라고 나옵니다. 한 번만 정하면 됩니다.

**시트에서 기록이 줄면 멈춥니다.** 실수로 줄이 지워진 것을 사이트에 그대로
반영하지 않기 위해서입니다. 정말 지우신 것이면 저에게 말씀해 주세요.

## 비밀번호는 어디에 있나

- `data/deploy.json` — FTP 비밀번호
- `data/youtube.json` — 유튜브 API 키
- `admin/config.php` — 관리자 계정 (서버에만 있습니다)

셋 다 **웹서버에도 GitHub 에도 올라가지 않습니다.** 이 PC 에만 있습니다.
