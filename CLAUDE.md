# 스타크래프트 기록실 — 작업 안내

이 폴더에서 일하는 Claude 를 위한 메모입니다. 휴대폰에서 짧게 시켜도
바로 알아듣도록 필요한 것만 적었습니다.

## 이게 뭔가

정적 사이트 세 개가 한 폴더에 있습니다.

| | 주소 | 내용 |
| --- | --- | --- |
| 끝장전 | `pubgin.com/endgame/` | 296매치 · 2,656세트 · 선수 35명 |
| ASL | `pubgin.com/endgame/asl/` | 23개 대회 · 1,299매치 · 2,177세트 · 선수 95명 |
| CG 제작 툴 | `pubgin.com/endgame/admin/` | 대진표 이미지 만들기 (관리자 로그인) |

**CG 제작 툴은 메뉴에 없습니다.** 페이지 맨 아래 톱니바퀴로만 들어갑니다.
일부러 눈에 안 띄게 둔 것이니 메뉴로 되돌리지 마세요.

**실제 사이트는 카페24의 pubgin.com 입니다.** GitHub 은 백업일 뿐이고,
거기 올린다고 사이트가 바뀌지 않습니다.

## 자주 쓰는 명령

새 PC 에서는 `START.bat` 을 더블클릭하면 끝납니다 (윈도우).
FTP 정보 네 가지만 묻고 유튜브 키 찾기·영상 채우기·빌드·업로드·백업까지
알아서 합니다. 자세한 건 `회사PC-설정.md` 에 있습니다.

명령으로 하시려면 이 둘입니다.

```bash
python3 tools/start.py     # START.bat 이 부르는 것. 처음부터 끝까지 한 번에
python3 tools/setup.py     # 준비(FTP 정보·접속 확인)만
```

그다음부터는 이것만 씁니다.

```bash
python3 tools/update.py              # 구글시트 → 데이터 → 사이트 → 업로드 (제일 많이 씀)
python3 tools/update.py --dry-run    # 아무것도 바꾸지 않고 무엇이 달라지는지만
python3 tools/build.py               # 사이트만 다시 만들기
python3 tools/deploy.py              # 만든 것을 FTP 로 올리기 (바뀐 파일만)
```

슬래시 명령으로도 됩니다 — `/갱신` `/확인` `/배포` `/상태`

## 작업이 끝나면 바로 올립니다

**사장님이 따로 말하지 않아도 올리세요.** 물어보지 말고 하시면 됩니다.
이 항목이 그 허락입니다.

- 화면·기능을 고쳤으면 → `python3 tools/build.py && python3 tools/deploy.py`
- 기록(시트)이 바뀐 것이면 → `python3 tools/update.py`
- 문서만 고쳤으면 → 올릴 것 없습니다

`deploy.py` 가 **접속 정보가 없다**고 하면 이 PC 는 아직 준비 전입니다.
`python3 tools/setup.py` 를 안내하고 넘어가세요.

`deploy.py` 가 **붙지 못하면** 지금 작업방이 클라우드일 수 있습니다.
클라우드에서는 pubgin.com 이 막혀 있어 무엇을 해도 안 됩니다. 그때는
"이 작업방에서는 서버에 못 닿습니다. 회사 PC 에서 `START.bat` 을 돌리시거나
PC 에서 `/remote-control` 로 이어 주세요" 라고 알려 주고 넘어가세요.
**억지로 우회하지 마세요.**

올린 뒤에는 무엇이 몇 개 올라갔는지 한 줄로 알려 주세요.

## 사이트가 바뀌면 슬랙으로 알립니다

`deploy.py` 가 파일을 올리고 나면 무엇이 바뀌었는지 슬랙에 올립니다.
PUBG META(`pubgapi`)의 `src/notify.js` 와 같은 방식입니다 — 웹훅 주소 하나로
글을 올리므로 계정 연동이나 2단계 인증이 필요 없습니다.

```
data/notify.json    무엇을 보낼지 (저장소에 올라갑니다)
data/slack.json     웹훅 주소 (저장소에 올리지 않습니다)
tools/notify.py     보내는 쪽. python3 tools/notify.py --test 로 시험 발송
```

- 웹훅 주소가 없으면 예전 PUBG META 폴더의 `.env` 에서 찾아와 저장합니다.
- **지금은 PUBG META 와 같은 채널로 갑니다.** 따로 받고 싶으시면 슬랙에서 새
  웹훅을 만들어 `data/slack.json` 의 `webhookUrl` 만 바꾸면 됩니다.
- 알림이 실패해도 배포는 이미 끝난 것이라 그냥 넘어갑니다. 끄려면
  `data/notify.json` 의 `enabled` 를 `false` 로.
- 바뀐 파일이 없으면 배포 자체를 건너뛰므로 알림도 가지 않습니다.

## 꼭 지킬 것

- **`index.html`, `p/`, `asl/`, `admin/`, `csv/`, `xlsx/`, `sheets.html` 은 빌드 결과물입니다.**
  직접 고치지 마세요. 고칠 곳은 `tools/` 와 `data/` 입니다.
- 화면을 바꾸려면 `tools/site.css`, `tools/app.js`, `tools/asl_app.js`,
  `tools/cg_app.js`, `tools/render.py` 를 고치고 `build.py` 를 돌립니다.
- `tools/build.py` 의 `if __name__ == '__main__'` 블록은 **항상 파일 맨 끝**에
  있어야 합니다. 그 뒤에 함수를 덧붙이면 NameError 가 납니다.
- **`data/deploy.json` 과 `admin/config.php` 는 저장소에 올리지 않습니다.**
  각각 FTP 비밀번호와 관리자 계정이 들어 있습니다.
- `update.py` 는 기록이 줄면 멈춥니다. 시트에서 줄이 지워지는 사고를 막기
  위해서입니다. 사용자가 의도한 삭제라고 확인해 주기 전에는 `--force` 를
  붙이지 마세요.
- **의도한 삭제를 반영할 땐 `9_삭제반영.bat`(= `tools/apply_delete.py`)** 을
  씁니다 — 무엇이 지워지는지 먼저 보여주고, 확인을 받은 뒤에만
  `update.py --force` 를 돌립니다. 갱신이 "데이터가 줄었습니다"로 멈추면
  이 버튼을 누르라고 콘솔·슬랙에 안내가 나옵니다. (2026-08-20 추가)

## 선수 사진

`img/players/<슬러그>.jpg` 를 넣고 빌드하면 선수 페이지와 CG 툴에 자동으로
붙습니다. 어떤 이름으로 넣어야 하는지는 빌드가 `img/players/_사진목록.txt` 에
써 둡니다. 없는 선수는 지금처럼 이름만 나옵니다.

CG 툴에서 선수 이름을 고르면 등록된 사진이 저절로 올라옵니다. 직접 올린
사진이 있으면 그쪽이 우선이고, 사진이 없는 선수로 바꾸면 앞 선수 얼굴은
지워집니다.

## 데이터가 어디서 오나

```
data/asl.json       ASL 정본  ← 구글시트에서 받아옴 (data/asl-source.json 에 주소)
data/endgame.json   끝장전 정본  ← 구글시트에서 받아옴 (data/endgame-source.json 에 주소)
data/videos.json    경기 → 유튜브 다시보기 매핑 (292/296 연결됨)
data/site.json      사이트 주소 (baseUrl)
```

둘 다 `update.py`(`/갱신`) 한 번으로 시트에서 받아옵니다. 끝장전은
`tools/endgame_import.py`, ASL 은 `tools/asl_import.py` 가 맡습니다.
**끝장전 시트도 기록이 줄면 멈춥니다** — ASL 과 같은 안전장치입니다.

빌드는 원본을 절대 고치지 않습니다. 나머지는 전부 다시 계산해서 만듭니다.

## 알아 둘 사실

- 끝장전은 사실상 9세트 고정입니다 (296경기 중 294경기).
- ASL 은 경기 날짜가 없어 대회·라운드 순서로만 정리합니다.
- ASL 동족전 세트는 종족 상성 집계에서 빠집니다.
- **랜덤 출전이 있습니다. 종족이 세트마다 다른 것을 오타로 보지 마세요.**
  이영호가 ASL Season 10 에서 랜덤으로 나와 9줄이 P·Z 로 적혀 있습니다.
  예전에는 이걸 오타로 보고 주 종족으로 덮어썼는데, 그러면 실제로 치른
  종족전이 사라집니다. 지금은 **세트에 적힌 종족을 그대로 쓰고**, 선수 목록에
  보이는 종족만 주 종족(가장 많이 뛴 종족)으로 표시합니다
  (`tools/asl_import.py` 의 `player_races`).
- ASL `상대 전적` 탭은 `asl.json` 의 `setList` 를 씁니다. 세트 하나하나를
  숫자로 압축한 것인데, 그 안의 번호는 `players`·`maps`·`tournaments` 배열의
  자리 번호입니다. **그 배열들의 순서를 바꾸면 전적이 어긋납니다.**
- 다시보기 영상은 292/296 경기가 연결돼 있습니다 (`tools/fetch_videos.py`).
  제목에 두 선수 이름이 다 나오는 영상만 인정하고, 영상 하나는 경기 하나에만
  붙입니다. 1~3년 뒤에 올라온 재업로드는 제목의 방송 회차(`SC1-N`)로 찾습니다.
  남은 4경기는 사람이 주소를 넣어 주면 됩니다.
- **예전 Node 프로젝트(`C:\Users\ETALENT-NEW\scetalent`)가 같은
  `/www/endgame/index.html` 에 15분마다 덮어쓰고 있었습니다.** 작업 스케줄러의
  `SC Endgame Cafe24 Mirror` 를 껐습니다(2026-08-19). 다시 켜지면 사이트가
  15분마다 예전 화면으로 뒤바뀝니다.

## 자세한 내용

**처음 이 폴더를 맡으셨다면 `인수인계.md` 부터 읽으세요.** 무엇이 되어 있고
무엇이 안 되어 있는지, 새 PC 에서 무엇을 다시 만들어야 하는지, 지금까지
잡은 버그가 무엇인지 정리돼 있습니다.

`README.md` 에 전부 있습니다. 빌드 구조, 도메인 옮기기, 관리자 로그인,
데이터 확보 방안까지 정리돼 있습니다.

## 시청자 승부예측 — 포인트 베팅 (2026-08-21 전면 개편)

**화면·안내 문구에 "토토"라는 단어는 쓰지 않습니다** (사용자 지시) — 항상
"시청자 승부예측"으로 표기. 내부 코드·API 이름(toto_*)만 그대로입니다.

시청자가 채팅으로 가상 포인트를 걸고 배당을 받는, 잉여력 토토식 이벤트입니다.

- **하루 흐름**: 관제 '오늘 개장' → 시청자 채팅 '도전'(1인 10,000P) →
  세트마다 '베팅 오픈' → 채팅 '선수이름 금액'/'선수이름 올인'(**첫 베팅
  고정**, 오탈자·형식 이탈은 미접수) → 마감 → 결과 → **합동배당**(총풀÷승자풀,
  정수 내림, 승자 없으면 전원 환불) → '하루 마감'에 최종 포인트 1위 우승.
  **파산(0P)=그날 끝.** 접수 ✅/실패 ❌(사유)는 피드로 관제·방송 장면·공개
  페이지에 실시간 표시(채팅 봇 답장은 SOOP 구조상 불가).
- **화면**: 관제 🎰 승부토토 패널 · 방송 장면 kind=toto(실시간 풀·배당·피드),
  toto_result(배당·적중·상위 획득), toto_champ(오늘의 우승) · 공개
  **predict.php**(오늘 순위/접수 확인/시즌 종합 — 우승·승률·누적P).
- **저장**(admin/pz/): toto_day.json(오늘 판, 4초 저장 — 새로고침 이어짐),
  toto_season.json(시즌 누적). **정산·하루마감은 서버(prize_api.php)가 단독
  수행** — 같은 라운드가 서버에 있을 때만 처리해 이중 지급 방지.
  toto_public 만 무로그인 공개. 연습(타 채널/?demo)은 관제 안 휘발성.
- 예전 predict_* 액션·데이터는 폐기됨 (파일은 서버에 잔존, 무해).
- **설계 원칙(2026-08-21 확정): 시청자 입력은 오직 채팅, 웹·방송 화면은
  결과 표시 전용.** 그래서 채팅을 정확히 읽는 게 생명이고, 이를 위해
  ① 예측 진행 중 **전 채팅 원본+운영 마커를 서버에 기록**
  (admin/pz/toto_chatlog-날짜.jsonl, 4초 플러시)
  ② 채팅 연결이 끊겼다 붙으면 몇 초 끊겼는지 피드로 경고(방송에서
  재베팅 안내용) + gap 마커 기록
  ③ **python3 tools/toto_verify.py [날짜]** — 로그를 같은 규칙으로
  리플레이해 실제 순위와 대조(불일치 줄 단위 리포트, --file 로 로컬 시험).
  한계: 채팅 리더가 관제 브라우저 하나라, 끊긴 구간은 로그에도 없음 —
  gap 경고로 즉시 알아채는 게 대응책.

## SOOP 채팅 선물 프로토콜 — 공식 확정 (2026-08-21)

SOOP 웹플레이어 번들(LivePlayer.js)의 공식 상수·파서를 직접 확인해 확정한
내용입니다. **별풍선 집계는 이 표가 기준이고, 추측으로 바꾸지 마세요.**

| svc | 공식 이름 | 뜻 | 집계 |
|---|---|---|---|
| 18 | SVC_SENDBALLOON | **별풍선** — [1]채널 [2]보낸이ID [3]닉 [4]개수 [5]팬클럽순번 [8]시그니처 이미지 파일명 | ✅ 별풍선 |
| 33 | SVC_SENDBALLOONSUB | 중계방 별풍선 — [2]채널 [4]ID [5]닉 [6]개수 | ✅ 별풍선 |
| 109 | SVC_OGQ_EMOTICON | OGQ 이모티콘 스티커 (채팅 꾸미기) | ❌ 별풍선 아님 |
| 87 | SVC_ADCON_EFFECT | 애드벌룬 | ❌ |
| 105 | SVC_VIDEOBALLOON | 영상풍선 | ❌ |
| 37 | SVC_CHOCOLATE | 초콜릿 | ❌ |
| 108 | SVC_SENDSUBSCRIPTION | 구독 | ❌ |
| 121/125 | SVC_MISSION(_SETTLE) | 도전미션 | ❌ |
| 30/39 | SVC_TOPFAN/TOPCLAN | 팬 순위(누적) | ❌ |
| 88 | SVC_CLOSE_BROAD | 방송 종료 신호 | — |

- 한때 svc109 를 별풍선으로 오인해 아이템ID 블랙리스트(EMOTE_ITEMS)로
  걸렀지만, **109는 애초에 OGQ 이모티콘이라 통째로 안 세는 게 정답** —
  블랙리스트는 폐기했습니다.
- svc18 의 `10_00040001` 같은 [8]값은 **시그니처 별풍선 이미지 파일명**
  (진짜 별풍선 맞음). **닉==ID 시청자도 진짜 별풍선**입니다(공식 파서에
  제외 규칙 없음) — 한때 넣었던 닉==ID 제외는 오판이라 제거했습니다.
- 적용 위치: 관제(prize.php)·soopchat.py·별풍선 확인 창(prize_balloon_debug.php).
